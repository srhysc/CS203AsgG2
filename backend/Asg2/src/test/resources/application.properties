# Test configuration
# Enable lazy initialization for faster unit tests
spring.main.lazy-initialization=true

# Disable banner
spring.main.banner-mode=off

# Disable web environment for tests (speeds up unit tests)
spring.main.web-application-type=none
