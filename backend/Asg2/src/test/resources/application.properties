# Test configuration to prevent Spring Boot from starting
spring.main.lazy-initialization=true
spring.autoconfigure.exclude=com.cs203.grp2.Asg2.config.FirebaseConfig

# Disable banner
spring.main.banner-mode=off

# Disable web environment for tests
spring.main.web-application-type=none
